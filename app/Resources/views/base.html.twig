<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Jean Forteroche{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('vendor/bootstrap/css/bootstrap.min.css') }}">
            <link rel="stylesheet" href="{{ asset('vendor/jquery-ui-1.12.0/jquery-ui.css') }}">
            <link rel="stylesheet" href="{{ asset('css/normalize.css') }}">
            <link href="https://fonts.googleapis.com/css?family=Titillium+Web:300,400" rel="stylesheet">
            <link rel="stylesheet" href="{{ asset('vendor/fontawesome/css/font-awesome.min.css') }}">
            <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% endblock %}
    </head>
    <body>
        <div class="container">
            {% block nav %}
                <div id="header">
                    <div class="text-center col-xs-12 col-xs-2 col-xs-offset-5">
                        <img src="{{ asset('img/typewriter.jpeg') }}" alt="machine à écrire" class="img-circle img-responsive" id="typewriter">
                    </div>
                    <div class="row col-md-offset-2 col-md-8 col-xs-12 text-center">
                        <h1 id="logo">Jean FORTEROCHE</h1>
                        <p>Bienvenue sur le blog où je publie par épisode mon prochain livre:</p>
                        <p><em>---- "Billet simple pour l'Alaska" ----</em></p>
                        <hr>
                    </div>

                    <div class="row col-xs-12 col-md-offset-2 col-md-8 ">
                        <a href="{{ path("cms_homepage")}}"> <span class="fa fa-home" aria-hidden="true"></span> </a> |
                        <a href="{{ path('cms_apropos') }}">à propos</a>

                        {#TODO "Login" link should be removed for the final version of the site#}
                        <a href="{{ path("login") }}" class="identification">Connexion</a>

                        {% if is_granted("ROLE_USER") %}
                            | <a href="{{ path("cms_page_new") }}"> Créer un billet </a> |
                            <a href="{{ path("cms_category_index") }}"> Catégories </a>
                            {% if is_granted("ROLE_ADMIN") %}
                                | <a href="{{ path("cms_comment_index") }}">Commentaires</a> |
                                <a href="{{ path('cms_user_index') }}">Utilisateurs</a>
                            {% endif %}
                            <a href="{{ path("logout") }}" class="identification"> Déconnexion <span id="deconnexion">|</span> </a>
                        {% endif %}
                        <hr>
                    </div>

                </div>
            {% endblock %}
            {% block body %}
            
            {% endblock %}

            {% block footer %}
                <footer class="col-md-offset-2 col-md-8 text-center">
                    <br>
                    <p>© 2016 - Forteroche</p>
                    <p>Made by <a href="#">Framaru</a></p>

                </footer>
            {% endblock %}
        </div>
        {% block javascripts %}
            <script src="{{ asset('https://cdnjs.cloudflare.com/ajax/libs/jquery/2.2.4/jquery.min.js') }}"></script>
            <script src="{{ asset('vendor/bootstrap/js/bootstrap.min.js') }}"></script>
            <script src="{{ asset('vendor/jquery-ui-1.12.0/jquery-ui.min.js') }}"></script>
            <script src="{{ asset('js/main.js') }}"></script>
        {% endblock %}
    </body>
</html>
