{% extends "::base.html.twig" %}

{% block body %}
 {% for page in pages | reverse %}
     <div class="page container col-md-offset-2 col-md-8">
         <h2>{{ page.title }}</h2>
         <p>
             <span><small><em>Posté le {{ page.postedAt | date('d/m/Y') }}</em></small></span>
             &nbsp;&nbsp;
             <span class="label label-default">{{ page.category.name }}</span></p>

         <br>

         <p>
             {{ page.content | striptags | slice(0,500) }}...
         </p>
         <p>
             <a href="{{ path("cms_page_display", { id: page.id }) }}"> continuer</a>
         </p>
     </div>
 {% endfor %}
{% endblock %}
